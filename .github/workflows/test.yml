name: Mon premier test
on: 
  push:
    branches:
      - main

jobs:
  affichage-et-liste:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v3

      - name: Affichage d'un message
        run: echo "Hello Github Actions!"
      - name: Affichage de la liste des fichiers
        run: ls -la